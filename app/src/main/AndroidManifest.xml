<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".ToDoListApplication"
        android:allowBackup="true"
        android:appCategory="productivity"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ToDoList">
        <activity
            android:name=".view.TaskViewActivity"
            android:exported="true"
            android:label="@string/task_view"
            android:parentActivityName=".view.TaskViewListActivity" />
        <activity
            android:name=".view.EditFilterCriterionActivity"
            android:exported="true"
            android:label="@string/edit_filter"
            android:parentActivityName=".view.TaskViewActivity" />
        <activity
            android:name=".view.TaskViewListActivity"
            android:label="@string/task_views"
            android:exported="true"
            android:parentActivityName=".view.MainActivity" />
        <activity
            android:name=".view.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>